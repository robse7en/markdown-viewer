<Window x:Class="MarkdownViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Markdown Viewer"
        Height="800" Width="1000"
        AllowDrop="True"
        Drop="OnWindowDrop"
        DragOver="OnWindowDragOver">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F3F3F3" BorderBrush="#DDDDDD" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="8,6">
                <Button x:Name="OpenButton" Content="Open…" Padding="12,4" Click="OnOpenClick" />
                <Button x:Name="ThemeButton" Content="Dark mode" Padding="12,4" Margin="8,0,0,0" Click="OnThemeToggleClick" />
                <TextBlock x:Name="FileNameText" VerticalAlignment="Center" Margin="16,0,0,0" Foreground="#555555" TextTrimming="CharacterEllipsis" />
            </StackPanel>
        </Border>

        <wv2:WebView2 x:Name="WebView" Grid.Row="1" />
    </Grid>
</Window>
